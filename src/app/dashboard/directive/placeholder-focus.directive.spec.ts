import { PlaceholderFocusDirective } from './placeholder-focus.directive';

describe('PlaceholderFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaceholderFocusDirective();
    expect(directive).toBeTruthy();
  });
});
